{{FILE_HEADER}}

package {{PACKAGE}};

{{IMPORTS}}

/**
 * {{FSM_DESCRIPTION}}
 * 
 * @author {{FSM_AUTHOR}}
 */
class {{FSM_NAME}} extends {{BASE_CLASS}} {
{{DECLARATIONS_STATES}}
{{DECLARATIONS_TRANSITIONS}}

	/**
	 * Create the object, build the state machine.
	 * 
	 * @param name
	 *            the name of the state machine
	 * @throws FsmException
	 *             if building of state machine fails
	 */
	public {{FSM_NAME}}(String name) throws FsmException {
		super(name);

{{CONSTRUCTOR_BODY_STATES}}
{{CONSTRUCTOR_BODY_TRANSITIONS}}
		setStartState({{START_STATE}});
	}
}
